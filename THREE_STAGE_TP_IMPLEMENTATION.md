# 三级止盈完整实现说明

## ✅ 已修复的问题

之前的问题：
- ❌ `state.py` 使用原版 `ExitRules`（只有单级止盈 tp1）
- ❌ 增强版 `EnhancedExitRules` 虽然定义了 tp1/tp2/tp3，但没有被使用

现在的修复：
- ✅ `state.py` 现在默认使用 `EnhancedExitRules`
- ✅ 所有品种都会使用三级止盈规则
- ✅ 增强版执行器会处理 tp1/tp2/tp3 三个级别

---

## 📊 三级止盈配置

### 标准档（normal）
```python
EnhancedExitRules(
    # 三级止盈
    tp1_pct=1.0,    # 浮盈达1%时触发
    tp1_ratio=30.0, # 平掉30%仓位
    
    tp2_pct=2.0,    # 浮盈达2%时触发
    tp2_ratio=40.0, # 再平掉40%仓位（累计已平70%）
    
    tp3_pct=3.5,    # 浮盈达3.5%时触发
    tp3_ratio=30.0, # 剩余全平
    
    # 智能止损
    sl_mode="st",           # 使用SuperTrend线
    sl_buffer_atr=0.5,      # 外扩0.5倍ATR
    sl_min_pct=1.2,         # 最小距离1.2%
    move_sl_to_entry=True,  # 第一档止盈后移到开仓价
    trail_with_st=True,     # 跟随超趋线
    
    # 盈利保护
    protect_profit_at=1.5,  # 浮盈达1.5%时启动保护
    protect_trail_pct=0.8,  # 允许回撤0.8%
)
```

### 快进快出档（quick）
```python
EnhancedExitRules(
    # 单级止盈（快速离场）
    tp1_pct=0.8,     # 浮盈0.8%就全平
    tp1_ratio=100.0, # 全部平掉
    
    tp2_pct=999,     # 不使用
    tp2_ratio=0,
    
    tp3_pct=999,     # 不使用
    tp3_ratio=0,
    
    # 固定止损
    sl_mode="pct",          # 固定百分比
    sl_pct=1.0,             # 1%止损
    sl_buffer_atr=0.3,      # 小缓冲
    sl_min_pct=1.0,
    move_sl_to_entry=False, # 不移动
    trail_with_st=False,    # 不跟随
    
    # 不使用盈利保护
    protect_profit_at=999,
    protect_trail_pct=0,
)
```

---

## 🔄 工作流程

### 标准档三级止盈示例

假设开多 BTC @ 60000，保证金 $10，杠杆 3x，名义价值 $30

#### 阶段1：开仓
```
开仓价: 60000
数量: 0.0005 BTC (名义价值 $30)
初始止损: 59280 (ST线外扩0.5*ATR)
```

#### 阶段2：价格涨到 60600 (+1%)
```
触发条件: 浮盈 1% >= tp1_pct (1%)
动作: 平掉30%仓位
  - 平仓量: 0.00015 BTC
  - 盈利: $0.09 (0.00015 * 600)
  
剩余仓位: 0.00035 BTC (70%)
止损调整: 移到开仓价 60000 (保本)
```

#### 阶段3：价格涨到 61200 (+2%)
```
触发条件: 浮盈 2% >= tp2_pct (2%)
动作: 平掉40%仓位（基于初始仓位）
  - 平仓量: 0.0002 BTC
  - 盈利: $0.24 (0.0002 * 1200)
  
剩余仓位: 0.00015 BTC (30%)
止损调整: 移到 60600 (锁定1%利润)
```

#### 阶段4：价格涨到 62100 (+3.5%)
```
触发条件: 浮盈 3.5% >= tp3_pct (3.5%)
动作: 剩余全平
  - 平仓量: 0.00015 BTC
  - 盈利: $0.315 (0.00015 * 2100)
  
总盈利: $0.645
已实现: 全部离场
```

---

## 📝 日志输出

启用三级止盈后，日志会显示：

```
[持仓建立] BTC-USDT-SWAP long @ 60000（保证金 10U × 3x，止损 59280，标准档 止盈 1% 平 30%）

[第1档止盈] BTC-USDT-SWAP 平 0.00015 @ 60600，止损→60000

[第2档止盈] BTC-USDT-SWAP 平 0.0002 @ 61200，止损→60600

[第3档止盈] BTC-USDT-SWAP 平 0.00015 @ 62100，止损→60000

[持仓结束] BTC-USDT-SWAP 第3档止盈全平，本笔已实现 +0.645 USDT
```

---

## 🎯 三级止盈的优势

### vs 单级止盈（原版 1.5%平70%）

| 场景 | 原版 | 三级止盈 |
|------|------|---------|
| 小波动（1-1.5%） | ❌ 不止盈 | ✅ 1%平30% |
| 中波动（1.5-2%） | ✅ 1.5%平70% | ✅ 1%平30% + 2%平40% |
| 大波动（3%+） | ⚠️ 已平70%，只剩30%吃趋势 | ✅ 1%/2%/3.5%逐步离场 |

### 优势总结

1. **小盈利快速落袋**
   - 1%就平30%，避免回吐
   - 原版要等到1.5%才动作

2. **大趋势持有更久**
   - 还有70%仓位参与2%和3.5%
   - 分批离场不会一次清空

3. **心理压力更小**
   - 每次止盈都有收获
   - 不会纠结"要不要全平"

4. **风险控制更好**
   - 第一档后就保本
   - 第二档后锁定1%利润
   - 剩余仓位无风险持有

---

## ⚙️ 针对不同品种的调整

### BTC/ETH（主流币，波动较小）
```python
tp1_pct=0.8,  tp1_ratio=30,   # 更容易触发
tp2_pct=1.5,  tp2_ratio=40,
tp3_pct=3.0,  tp3_ratio=30,
```

### SOL/AVAX（山寨币，波动大）
```python
tp1_pct=1.5,  tp1_ratio=30,   # 等大一点再平
tp2_pct=3.0,  tp2_ratio=40,
tp3_pct=5.0,  tp3_ratio=30,
```

### MU等小盘币（波动极大）
```python
tp1_pct=1.2,  tp1_ratio=40,   # 第一档多平一些
tp2_pct=2.5,  tp2_ratio=40,
tp3_pct=4.0,  tp3_ratio=20,   # 留少量博大趋势
```

---

## 🔍 如何验证三级止盈生效

### 1. 查看日志
```bash
tail -f backend.log | grep "档止盈"
```

应该看到：
```
[第1档止盈] ...
[第2档止盈] ...
[第3档止盈] ...
```

### 2. 前端持仓卡
持仓事件中会显示：
```
✓ 第1档止盈 0.00015 @ 60600（30% 仓位）
✓ 止损移至开仓价 60000（保本）
✓ 第2档止盈 0.0002 @ 61200（40% 仓位）
✓ 止损上移至 60600（锁定1%利润）
```

### 3. 订单记录
API `/api/orders` 会显示：
```json
{
  "kind": "tp1",
  "stage": 1,
  "profile": "normal",
  "reason": "浮盈 1.02% ≥ 1%，第1档止盈"
}
{
  "kind": "tp2",
  "stage": 2,
  "profile": "normal",
  "reason": "浮盈 2.03% ≥ 2%，第2档止盈"
}
{
  "kind": "tp3",
  "stage": 3,
  "profile": "normal",
  "reason": "浮盈 3.52% ≥ 3.5%，第3档止盈"
}
```

---

## 🐛 故障排查

### Q: 日志只显示"第1档止盈"，没有第2、3档
**A**: 可能原因：
1. 价格没有继续上涨到 tp2_pct
2. 在达到 tp2 前就反向平仓了
3. 在达到 tp2 前触发止损

这是正常的，不是每笔单都会走完三档。

### Q: 想改为两级止盈
**A**: 修改配置：
```python
tp1_pct=1.0, tp1_ratio=50,  # 第一档平50%
tp2_pct=2.5, tp2_ratio=50,  # 第二档全平
tp3_pct=999, tp3_ratio=0,   # 禁用第三档
```

### Q: 想改回单级止盈
**A**: 有两个方法：

方法1：修改比例
```python
tp1_pct=1.5, tp1_ratio=100,  # 1.5%全平
tp2_pct=999, tp2_ratio=0,    # 禁用
tp3_pct=999, tp3_ratio=0,    # 禁用
```

方法2：在 `state.py` 中切回原版
```python
from position import ExitRules  # 改回原版
# from position_enhanced import EnhancedExitRules
```

---

## 📈 预期效果

启用三级止盈后：
- ✅ 小盈利单增加（1%就开始止盈）
- ✅ 大盈利单也能抓住（还有70%持仓）
- ✅ 止损次数减少（第一档后就保本）
- ✅ 平均盈亏比改善
- ✅ 心理压力减小（分批离场）

---

**现在三级止盈已完全集成，重启服务后生效！** 🚀
