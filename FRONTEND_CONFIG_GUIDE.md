# 前端三级止盈配置界面已添加

## ✅ 已完成的修改

### frontend/src/components/TradePanel.jsx

添加了以下配置区域：

#### 1. 三级止盈配置
```
📊 三级止盈（分批离场）

第一档 %:  [1.0] → 平 [30] %  [改]
第二档 %:  [2.0] → 平 [40] %
第三档 %:  [3.5] → 全平

💡 三级止盈示例：1%平30% → 2%平40% → 3.5%全平
小盈利快速落袋，大趋势持有更久，心理压力更小
```

#### 2. 智能止损配置
```
🛡️ 智能止损（减少震荡扫损）

ATR缓冲倍数: [0.5] × ATR
最小止损距离 %: [1.2] %

智能止损 = max(ST线-ATR缓冲, 开仓价×最小距离%)
避免震荡时被正常波动扫损
```

#### 3. 盈利保护配置
```
💎 盈利保护（避免过早离场）

启动保护 %: [1.5] %
允许回撤 %: [0.8] %

示例：浮盈达1.5%后，允许回撤0.8%
即从最高点回落超过0.8%才止损，避免短期回调就离场
```

---

## 🎨 界面效果

### 挂单页面布局

```
┌─────────────────────────────────────────┐
│ 止盈止损 · 标准档                        │
├─────────────────────────────────────────┤
│ 📊 三级止盈（分批离场）                  │
│                                         │
│ 第一档 %:  [1.0▼] → 平 [30▼] % [改]    │
│ 第二档 %:  [2.0▼] → 平 [40▼] %         │
│ 第三档 %:  [3.5▼] → 全平                │
│                                         │
│ 💡 三级止盈示例：1%平30% → 2%平40%...   │
├─────────────────────────────────────────┤
│ 🛡️ 智能止损（减少震荡扫损）              │
│                                         │
│ ATR缓冲倍数:  [0.5▼] × ATR              │
│ 最小止损距离: [1.2▼] %                  │
│                                         │
│ 智能止损 = max(ST线-ATR缓冲, ...)       │
├─────────────────────────────────────────┤
│ 💎 盈利保护（避免过早离场）              │
│                                         │
│ 启动保护 %: [1.5▼] %                    │
│ 允许回撤 %: [0.8▼] %                    │
│                                         │
│ 示例：浮盈达1.5%后，允许回撤0.8%...     │
├─────────────────────────────────────────┤
│ ☑ 止盈后止损抬到开仓价                   │
│                                         │
│ 初始止损: [超趋线] [百分比]             │
│ ☑ 跟随超趋线移动止损                     │
└─────────────────────────────────────────┘
```

---

## 🔧 使用方法

### 1. 调整三级止盈

**场景A：主流币（BTC/ETH）- 波动小**
```
第一档: 0.8% 平 30%
第二档: 1.5% 平 40%
第三档: 3.0% 全平
```

**场景B：山寨币（SOL/AVAX）- 波动大**
```
第一档: 1.5% 平 30%
第二档: 3.0% 平 40%
第三档: 5.0% 全平
```

**场景C：小盘币（MU等）- 频繁止损**
```
第一档: 1.0% 平 40%  ← 多平一些
第二档: 2.5% 平 40%
第三档: 4.0% 全平
```

### 2. 调整智能止损

**如果频繁被扫损：**
```
ATR缓冲倍数: 0.5 → 0.8  ← 加大缓冲
最小止损距离: 1.2% → 1.5%  ← 放宽距离
```

**如果想紧一点：**
```
ATR缓冲倍数: 0.5 → 0.3  ← 减小缓冲
最小止损距离: 1.2% → 1.0%  ← 收紧距离
```

### 3. 调整盈利保护

**激进型（快速获利了结）：**
```
启动保护: 1.5% → 1.0%  ← 更早启动
允许回撤: 0.8% → 0.5%  ← 更少回撤
```

**保守型（追求大趋势）：**
```
启动保护: 1.5% → 2.5%  ← 延后启动
允许回撤: 0.8% → 1.2%  ← 更多回撤空间
```

---

## 💾 配置保存

所有修改都会：
1. ✅ 实时生效（下一笔单立即使用新参数）
2. ✅ 自动保存到 `settings.json`
3. ✅ 重启后保留

---

## 🔍 检查配置是否生效

### 方法1：查看API响应
```bash
curl http://localhost:8000/api/settings | jq '.exit_rules.normal'
```

应该看到：
```json
{
  "enabled": true,
  "tp1_pct": 1.0,
  "tp1_ratio": 30.0,
  "tp2_pct": 2.0,
  "tp2_ratio": 40.0,
  "tp3_pct": 3.5,
  "tp3_ratio": 30.0,
  "sl_buffer_atr": 0.5,
  "sl_min_pct": 1.2,
  "protect_profit_at": 1.5,
  "protect_trail_pct": 0.8,
  ...
}
```

### 方法2：查看日志
```bash
tail -f backend.log | grep "档止盈"
```

应该看到三档止盈的日志：
```
[第1档止盈] BTC-USDT-SWAP 平 0.3 @ 60600
[第2档止盈] BTC-USDT-SWAP 平 0.4 @ 61200
[第3档止盈] BTC-USDT-SWAP 平 0.3 @ 62100
```

### 方法3：前端持仓卡
持仓事件中会显示：
```
tp1: 第一档止盈 0.3 @ 60600
tp2: 第二档止盈 0.4 @ 61200
tp3: 第三档止盈 0.3 @ 62100
```

---

## ⚠️ 注意事项

1. **第一档修改需要点击"改"按钮**
   - 因为涉及两个参数（止盈%和平仓比例）
   - 同时修改避免中间状态

2. **第二档和第三档实时生效**
   - 直接输入即可
   - 无需点击按钮

3. **快进快出档（quick）不支持三级止盈**
   - 只有单级：0.8%全平
   - 这是设计如此（快速离场）

4. **配置只影响新开仓位**
   - 已有持仓使用开仓时的规则
   - 不会影响正在持有的单子

---

## 🎯 推荐配置组合

### 组合A：稳健型（推荐新手）
```
三级止盈: 1% / 2% / 3.5%，平 30% / 40% / 30%
智能止损: ATR 0.5x，最小 1.2%
盈利保护: 1.5% 启动，允许回撤 0.8%
```

### 组合B：激进型（适合高波动品种）
```
三级止盈: 1.5% / 3% / 5%，平 30% / 40% / 30%
智能止损: ATR 0.8x，最小 1.5%
盈利保护: 2% 启动，允许回撤 1%
```

### 组合C：保守型（适合资金安全优先）
```
三级止盈: 0.8% / 1.5% / 2.5%，平 40% / 40% / 20%
智能止损: ATR 0.3x，最小 1%
盈利保护: 1% 启动，允许回撤 0.5%
```

---

## 📚 相关文档

- `THREE_STAGE_TP_IMPLEMENTATION.md` - 三级止盈实现详解
- `FINAL_CHECKLIST.md` - 完整功能清单
- `DEPLOYMENT_COMPLETE.md` - 部署说明

---

**前端配置界面已完成，重启前端后即可使用！** 🎉

刷新浏览器查看新的配置选项。
