一、整体架构（V3）
Market Regime Engine

                         4H K线

                           |

             Regime Detection（三概率）
           trend / range / transition

                           |
             ───────────────────────────
             |        |         |        |
          Trend   Range 类   Transition  Breakout
                   │
          ┌────────┼────────┐
      上涨整理   真正横盘   下跌整理
      (回踩低吸) (高抛低吸) (反弹做空)

核心思想（V3 关键升级）
─────────────────────────────
旧版把“震荡”当成单一状态 → 用纯均值回归处理所有横盘 → BTC 慢涨时反复高抛，被趋势碾。
V3 把震荡按方向细分为三类，并让“趋势是否真实”由 ER+ADX 决定（不再看 MA 斜率）：

  Market Regime = 6 态
  ──────────────────────────────────────────────────────
  状态              策略
  Trend             SuperTrend 趋势跟随
  Uptrend Consol    上涨整理：回踩低吸（禁止高抛）
  Neutral Range     真正横盘：高抛低吸（均值回归）
  Downtrend Consol  下跌整理：反弹做空（禁止低吸）
  Transition        压缩末端：监听突破
  Breakout          突破：放量+ATR扩张+ST方向一致，顺势介入
  ──────────────────────────────────────────────────────

直觉：完全放弃震荡=放弃大量市场时间。
     正确方向不是放弃 Range，而是不要把震荡简单等同于“均值回归”。
     真正要识别的是：“这是没有方向的震荡，还是趋势中的休息？”

模块1：进入状态识别（Regime Detection）
目标
提前发现市场处于哪一类状态，而不是等形态成形。

检测维度：
  方向效率(ER) / 趋势强度(ADX) / 均线方向(MA30斜率) / 波动状态(压缩) / 净漂移(Drift)

1. ER（Efficiency Ratio）效率比
   作用：判断价格走得有没有效率（趋势 vs 震荡）。
   公式：ER = abs(Close_now - Close_N) / Sum(abs(Close[i] - Close[i-1]))
   4H: N = 20
   评分：> 0.5 趋势 / 0.3-0.5 过渡 / < 0.3 震荡

2. MA30 趋势方向（不是“趋势力量”，只作方向参考）
   公式：MA30_slope = (MA30_now - MA30_10) / ATR
   评分：abs(slope) < 0.2 → 方向弱
   ⚠️ V3 重要变更：MA30 斜率只用于“震荡子分类”的方向判断，
      不再计入 trend 概率（否则慢涨会被误判为强趋势）。

3. ADX 趋势强度
   只判断趋势强弱，不判断方向。
   规则：ADX < 20 → 震荡概率增加；ADX > 25 → 趋势概率增加。

4. 波动压缩
   不用固定 Range，看变化：Compression = Range(20) / Range(50)
   结果 < 1 表示正在收缩。

5. Drift 净漂移（V3 新增，震荡子分类用）
   公式：Drift = (Close[i] - Close[i-DRIFT_K]) / ATR   （DRIFT_K = 20）
   含义：> 0 上行 / < 0 下行 / ≈ 0 横盘
   用于区分“横盘”与“趋势中的休息”。

Range Entry Score（三概率）
─────────────────────────
  trend    = 0.5 * ER_score + 0.5 * ADX_score
  range    = 0.5 * (1 - ER_score) + 0.25 * ADX_low + 0.25 * compression
  transition = max(趋势衰减, 模棱两可 * 0.8)
             趋势衰减 = 0.5*(ER衰减) + 0.5*(ADX衰减)

  ER_score   = clamp(ER / 0.4, 0, 1)
  ADX_score  = clamp(ADX / 25, 0, 1)
  ADX_low    = clamp((25 - ADX) / 25, 0, 1)
  compression= clamp(1 - Range20/Range50, 0, 1)

输出：
  {
    "trend_probability": 0.xx,
    "range_probability": 0.xx,
    "transition_probability": 0.xx,
    "drift": 0.xx
  }

模块2：震荡子分类（V3 核心）
当状态机判定为“非趋势 / 非过渡 / 非突破”时，进入震荡家族，
再用 slope/drift 细分为三类：

  classifyRangeState(i):
    if MA30_slope > 0.12  or  Drift > 1.2   → uptrend_consol   上涨整理
    if MA30_slope < -0.12 or  Drift < -1.2  → downtrend_consol 下跌整理
    else                                    → neutral_range   真正横盘

  条件对照：
  ───────────────────────────────────────────────────────
  状态            条件                                 策略
  Neutral Range   Drift≈0 · MA30水平 · ADX低        双边：支撑买 / 压力卖
  Uptrend Consol  Range + Drift>0 + MA30向上         禁止高抛；
                                                      只：支撑附近买；突破继续持有
  Downtrend Consol Range + Drift<0 + MA30向下        只：压力做空；
                                                      禁止低吸；跌破继续持有
  ───────────────────────────────────────────────────────

模块3：支撑/压力区（Support/Resistance Zone Engine）
目标：寻找市场反复交易认可的位置（区域，不是一条线）。
  Step1 Pivot 检测（4H 左右 3 根 K）
  Step2 价格聚类（|p1-p2| < 0.5 ATR 视为同区）
  Step3 区域强度评分（100 分）：
        触碰次数 30% · 反弹幅度 25% · 最近性 15% ·
        成交量 10% · 假突破拒绝 20%
  输出 low / high / center / strength / rejections

模块4：震荡衰竭检测（Exhaustion）
判断还能继续区间交易吗？
  边界测试 25% · ATR扩张 20% · 量恢复 15% ·
  区间扩大 10% · MA30重指向 15% · ER提升 15%
  Exhaustion Score：
    0-40   正常震荡
    40-70  震荡末端
    > 70   准备变盘（触发 Breakout 监听）

模块5：状态机 + 各态策略（V3 完整版）
─────────────────────────────────────
  function stateAt(i):
    if Exhaustion >= 70 且 突破确认(放量+ATR扩张+ST同向):
        return 'breakout'
    if trend_prob >= 0.5 且 ADX >= 20:
        return 'trend'
    if Exhaustion > 50:
        return 'transition'
    return classifyRangeState(i)   # 兜底 → 三类震荡之一

  各态交易权限（回测/实盘一致）：
  ───────────────────────────────────────────────────────
  Trend
    → SuperTrend Trend Agent：4h 为 trend 态时，1h ST flip 顺势换仓
       （上涨持多 / 下跌持空，翻转即反向）

  Neutral Range（真正横盘）
    → 高抛低吸（均值回归）
       支撑区下沿(反弹)买 / 压力区上沿(回落)卖
       过滤：价格进入区下沿<0.2 且区强度>60 且收阳才买；
            进入区上沿>0.8 且收阴才卖；Exhaustion<50 才做

  Uptrend Consol（上涨整理）
    → 只回踩低吸，禁止高抛
       支撑区下沿买（强度>55，收阳）
       支撑失守（收盘跌破支撑 low）→ 平多，等 Trend 态接管（不裸空）
       突破上行由 Breakout 态顺势介入，继续持有

  Downtrend Consol（下跌整理）
    → 只反弹做空，禁止低吸
       压力区上沿卖（强度>55，收阴）
       压力失守（收盘突破压力 high）→ 平空，等 Trend 态接管（不裸多）
       跌破下行由 Breakout 态顺势介入，继续持有

  Transition / Breakout（压缩末端 / 突破）
    → 禁止反向 Range 交易；仅监听突破
       突破压力区(放量>1.5× + ATR>1.2× + ST方向=多) → 买
       跌破支撑区(放量>1.5× + ATR>1.2× + ST方向=空) → 卖
       突破后 SuperTrend 重新接管（回到 Trend）

  同向自翻转抑制：已持多不再发 buy，已持空不再发 sell，
                  避免把“持有”误判为加仓而实现亏损。

最终放入 Quant OS：
  Market Regime Agent 输出：
  {
    "market_state": "uptrend_consol",
    "range_probability": 0.65,
    "trend_probability": 0.35,
    "drift": 1.8,
    "support_zone": 98000,
    "resistance_zone": 102000,
    "exhaustion_score": 38,
    "breakout_watch": false
  }

  状态              调用模块
  Trend             SuperTrend Trend Agent
  Uptrend Consol    Pullback Agent（回踩低吸）
  Neutral Range     Range Agent（高抛低吸）
  Downtrend Consol  Pullback Agent（反弹做空）
  Transition        Breakout Monitor
  Breakout          Breakout Agent
