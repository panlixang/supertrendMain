# 形态识别页 - 止盈止损快速指南

## 两种模式

### 模式 1：三档分批止盈（默认）
**配置**：TP3 = 3.5
**行为**：
- ✓ 浮盈 +1.0% → 平 30% 仓位，止损移到开仓价（保本）
- ✓ 浮盈 +2.0% → 再平 40% 仓位
- ✓ 浮盈 +3.5% → 平剩余 30% 仓位
- ✓ 保本后跟随 ST 线移动止损

**适合**：震荡行情、假突破频繁

---

### 模式 2：反向信号平仓
**配置**：TP3 = 0
**行为**：
- ✓ 忽略所有价格变动（TP1/TP2/TP3/止损全部失效）
- ✓ 只在下一个反向信号出现时全仓平掉
- ✓ 让利润充分奔跑

**适合**：强趋势行情、单边上涨/下跌

---

## 前端操作

1. 打开形态识别页（/pattern）
2. 右侧配置面板 → 找到"止盈止损"区域
3. 找到"TP3 幅度"输入框：
   - **设为 `0`** → 切换到反向信号平仓模式
   - **设为 `3.5`** → 切换到三档止盈模式
4. 配置立即生效（已开仓位继续使用开仓时的规则）

---

## 示例对比

### 场景：开仓价 100，当前价 110（浮盈 +10%）

| 模式 | TP3 配置 | 行为 |
|------|---------|------|
| 三档止盈 | 3.5 | 已触发 TP1/TP2/TP3，全部平仓 ✓ |
| 反向平仓 | 0 | 仍持有 100% 仓位，等反向信号 ✓ |

---

## 技术实现

**后端逻辑** ([position_enhanced.py:105](backend/position_enhanced.py#L105))：
```python
def check_enhanced(pos, price, rules, max_unrealized=None):
    if rules.tp3_pct <= 0:
        return None  # 跳过所有价格检查
    # ... 正常止盈止损逻辑
```

**API 字段**：
- `tp3_pct`：第三档止盈幅度（0 = 反向平仓模式）

---

## 注意事项

1. **风险提示**：反向平仓模式无止损保护，适合趋势明确的行情
2. **品种独立**：每个交易品种可单独配置不同模式
3. **持仓中切换**：已开仓位不受面板改动影响，保持开仓时的规则

---

更新时间：2026-09-26  
相关文档：[TP3_ZERO_SUMMARY.md](TP3_ZERO_SUMMARY.md)
