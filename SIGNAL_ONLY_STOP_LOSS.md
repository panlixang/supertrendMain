# 信号驱动止损配置说明

## 🎯 需求

**只用反向信号止损，不用固定百分比止损**

- ✅ 出现反向信号 **且** 符合下单逻辑（通过ER/等级/强度闸门）→ 平仓
- ✅ 出现假信号被屏蔽（不符合下单逻辑）→ 不平仓，继续持有
- ❌ 价格跌到止损线 → **不平仓**（等待信号）

## 📝 修改内容

### 1. 禁用价格止损规则

**修改文件**：`backend/state.py`

**标准档规则**：
```python
self.exit_rules = EnhancedExitRules(
    # 三级止盈（保留）
    tp1_pct=1.0, tp1_ratio=30.0,
    tp2_pct=2.0, tp2_ratio=40.0,
    tp3_pct=3.5, tp3_ratio=30.0,
    # 禁用价格止损 ← 新增
    enabled=False,  # 关闭价格驱动的止损
    sl_mode="st", sl_pct=2.0,
    sl_buffer_atr=0.5, sl_min_pct=1.2,
    move_sl_to_entry=True, trail_with_st=True,
    # 盈利保护（保留）
    protect_profit_at=1.5, protect_trail_pct=0.8,
)
```

**快进快出档规则**：
```python
self.exit_rules_quick = EnhancedExitRules(
    # 快速止盈（保留）
    tp1_pct=0.8, tp1_ratio=100.0,
    tp2_pct=999, tp2_ratio=0,
    tp3_pct=999, tp3_ratio=0,
    # 禁用价格止损 ← 新增
    enabled=False,  # 关闭价格驱动的止损
    sl_mode="pct", sl_pct=1.0,
    sl_buffer_atr=0.3, sl_min_pct=1.0,
    move_sl_to_entry=False, trail_with_st=False,
    protect_profit_at=999, protect_trail_pct=0,
)
```

### 2. 执行器忽略价格止损

**修改文件**：`backend/executor.py`

**on_price() 函数**：
```python
async def on_price(self, price: float):
    pos = self.store.position
    if not pos or pos.qty <= 0 or not price:
        return

    rules = self.rules_of(pos)

    # 如果止损规则被禁用，只处理止盈，不处理止损
    if not rules.enabled:
        # 只检查止盈，不检查止损
        if USE_ENHANCED and isinstance(rules, EnhancedExitRules):
            pos.update_max_unrealized(price)
            act = check_enhanced(pos, price, rules, pos.max_unrealized_pct)
            if act and act.get("action") == "stop":
                # 规则禁用时忽略止损信号
                logger.debug(f"[止损忽略] 价格触及止损线但规则已禁用，等待反向信号")
                return
        # ... 原版检查类似
    else:
        # 正常流程：检查止盈和止损
        # ... 原有代码
```

### 3. 信号驱动止损（已存在）

**已有逻辑**（`executor.py` 第108-113行）：
```python
# 反向信号平仓：只认「与持仓同周期」或「自身通过闸门要下单」的信号
if (pos and pos.qty > 0 and pos.side != want
        and (sig["tf"] == pos.tf or gate.get("trade"))):
    await self._close(pos, f"{sig['tf']} 出现反向信号", sig.get("price"))
```

**闸门判定**（`regime.py` `evaluate()` 函数）：
```python
gate = {
    "trade": True/False,  # 是否通过闸门
    "reasons": [...],     # 未通过的原因
    "hidden": bool,       # 是否静默
}
```

**通过闸门的条件**（全部满足）：
1. ✅ ER ≥ er_min（不是震荡市）
2. ✅ 信号等级 ∈ allow_grades（默认只做A/B）
3. ✅ 强度 score ≥ min_score（翻转要干脆）
4. ✅ tf ∈ allow_tfs（默认只做15m以上）

## 🔄 工作流程

### 开仓后

```
1. 开仓：BTC 10x 多头 @ 70,000
   止损线：计算但不使用（只记录在持仓对象里）

2. 价格下跌到 67,900（-3%，触及止损线）
   ❌ 不平仓（enabled=False，忽略价格止损）
   ✅ 继续持有，等待反向信号

3. 价格继续下跌到 65,000（-7.1%）
   ❌ 仍不平仓
   ✅ 继续等待反向信号

4. 出现反向信号（15m 卖出信号）
   → 检查闸门：
   
   场景A：假信号（被屏蔽）
   - ER = 0.12（< 0.15，震荡市）
   - gate["trade"] = False
   - 信号TF ≠ 持仓TF
   → ❌ 不平仓（假信号不止损）
   
   场景B：真信号（通过闸门）
   - ER = 0.28（≥ 0.15，趋势市）
   - 等级 = A
   - 强度 = 3
   - gate["trade"] = True
   → ✅ 平仓（"15m 出现反向信号"）
   
   场景C：同周期信号（即使没通过闸门）
   - 持仓来自 15m 信号
   - 当前 15m 出现反向信号
   - 即使 gate["trade"] = False
   → ✅ 平仓（同级别翻转优先）
```

### 止盈逻辑（保留）

价格止盈 **继续工作**（只是止损被禁用）：

```
1. 价格涨到 70,700（+1%）
   ✅ 第1档止盈：平 30%
   
2. 价格涨到 71,400（+2%）
   ✅ 第2档止盈：再平 40%
   
3. 价格涨到 72,450（+3.5%）
   ✅ 第3档止盈：剩余全平
```

## 📊 对比

### 修改前（价格止损）

| 场景 | 价格变动 | 行为 | 结果 |
|------|---------|------|------|
| 震荡下跌 | -3% | 触及止损线 | ❌ 立即止损 |
| 假突破 | -5% | 触及止损线 | ❌ 立即止损 |
| 深回调 | -8% | 触及止损线 | ❌ 早被止损 |
| 反向信号 | -2% | 通过闸门 | ✅ 信号止损 |

**问题**：震荡、假突破都会被价格止损扫掉。

### 修改后（信号止损）

| 场景 | 价格变动 | 行为 | 结果 |
|------|---------|------|------|
| 震荡下跌 | -3% | 价格触及止损线 | ✅ 忽略，继续持有 |
| 假突破 | -5% | 价格触及止损线 | ✅ 忽略，继续持有 |
| 深回调 | -8% | 价格触及止损线 | ✅ 忽略，等反向信号 |
| 假信号 | -4% | 反向信号但被屏蔽 | ✅ 不平仓 |
| 真信号 | -2% | 反向信号通过闸门 | ✅ 信号止损 |
| 同周期翻转 | -1% | 同周期反向信号 | ✅ 立即止损 |

**优势**：
- ✅ 不怕震荡和假突破
- ✅ 假信号不会止损
- ✅ 只有真正的趋势翻转才止损
- ✅ 同周期翻转优先（防止趋势反转时犹豫）

## ⚠️ 风险提示

### 1. 无止损保护

**风险**：如果一直没有反向信号，价格可能大幅亏损。

**示例**：
```
开仓：70,000 做多
价格：70,000 → 65,000 → 60,000 → 55,000
       -7%      -14%     -21%
       
如果一直没有反向信号出现，持仓会一直亏损，直到：
1. 出现反向信号且通过闸门
2. 或者手动平仓
```

**缓解措施**：
- 确保信号系统灵敏（趋势反转时能及时产生信号）
- 前端监控持仓浮亏，超过阈值时人工介入
- 可以保留止盈（已保留），至少能止盈离场

### 2. 信号延迟

**风险**：SuperTrend 是趋势跟随指标，翻转时会有延迟。

**示例**：
```
顶部：72,000（趋势线还是多头）
下跌：72,000 → 70,000 → 68,000
      此时 SuperTrend 才翻转为空头
      
实际止损价格：68,000
理想止损价格：70,000
延迟损失：2,000（-2.8%）
```

**缓解措施**：
- 闸门配置中提高 `min_score`（翻转要干脆）
- 降低 `er_min`（允许更早的翻转信号）
- 使用较小周期作为保护（如持仓来自15m，也监听5m的翻转）

### 3. 震荡市连续假信号

**风险**：震荡市中可能反复开平。

**示例**：
```
15m 信号A：买入 @ 70,000（ER=0.16，通过闸门）
15m 信号B：卖出 @ 69,500（ER=0.17，通过闸门）→ 止损
15m 信号C：买入 @ 70,200（ER=0.16，通过闸门）→ 再开仓
15m 信号D：卖出 @ 69,800（ER=0.15，通过闸门）→ 再止损

连续止损 4 次
```

**缓解措施**：
- 提高 `er_min`（如从 0.15 提到 0.18）
- 启用组合过滤器（ATR、MTF、ADX）
- 提高 `min_score`（从 2 提到 3）
- 增加 `cooldown_sec`（同周期两次信号最小间隔）

## 🎛️ 配置调整建议

### 激进配置（信号灵敏）

```python
# regime.py / TradeConfig
er_min = 0.12         # 降低门槛，更早响应翻转
min_score = 2         # 保持标准
allow_grades = ["A", "B", "C"]  # 接受更多信号
cooldown_sec = 180    # 3分钟冷却
```

**特点**：
- ✅ 趋势翻转时能快速止损
- ❌ 震荡市可能连续假信号

### 保守配置（减少假信号）

```python
er_min = 0.20         # 提高门槛，只在明确趋势时交易
min_score = 3         # 只接受最干脆的翻转
allow_grades = ["A", "B"]  # 拒绝C级（逆MTF）
cooldown_sec = 600    # 10分钟冷却
```

**特点**：
- ✅ 假信号大幅减少
- ❌ 真反转时可能止损延迟（多亏2-3%）

### 推荐配置（平衡）

```python
er_min = 0.15         # 标准门槛
min_score = 2         # 标准强度
allow_grades = ["A", "B"]  # 只做顺MTF
cooldown_sec = 300    # 5分钟冷却

# 启用组合过滤器
atr_filter_enabled = True
mtf_filter_enabled = True
```

**特点**：
- ✅ 平衡信号质量和响应速度
- ✅ 组合过滤减少假信号

## 🚀 验证方法

### 1. 查看日志

重启后，开仓时会看到：
```
[持仓建立] BTC-USDT-SWAP long @ 70000 保证金 10U × 10x 止损 67818 档位 normal
```

价格下跌触及止损线时：
```
[止损忽略] 价格触及止损线但规则已禁用，等待反向信号
```

出现反向信号时：
```
[平仓] BTC-USDT-SWAP 15m 出现反向信号，平仓 0.0005 @ 69500
```

### 2. 观察行为

- ✅ 价格大幅下跌但没有反向信号 → 不平仓
- ✅ 出现假信号（被闸门屏蔽）→ 不平仓
- ✅ 出现真信号（通过闸门）→ 平仓
- ✅ 同周期反向信号 → 立即平仓

### 3. 前端显示

持仓卡应该显示：
```
持仓：BTC 10x 多头 @ 70,000
当前价：68,500（-2.14%）
止损方式：信号止损（价格止损已禁用）
等待：15m 卖出信号（通过闸门）
```

## 📝 总结

**现在的逻辑**：
- ✅ **价格止损**：完全禁用（`enabled=False`）
- ✅ **价格止盈**：继续工作（三级止盈 1%/2%/3.5%）
- ✅ **信号止损**：
  - 反向信号 + 通过闸门 → 平仓
  - 反向信号 + 被屏蔽 → 不平仓
  - 同周期反向信号 → 立即平仓

**重启服务，配置生效！** 🚀
