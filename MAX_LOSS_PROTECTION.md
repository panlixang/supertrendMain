# 极端保护止损功能说明

## 🎯 目的

在**信号驱动止损**的基础上，增加**极端保护止损**（最大亏损止损），防止以下极端情况：

### 极端场景

1. **信号系统失效**
   - SuperTrend 指标计算异常
   - WebSocket 信号推送中断
   - 闸门配置过严（ER过高），反向信号一直被屏蔽

2. **市场剧烈波动**
   - 黑天鹅事件（如 FTX 暴雷）
   - 闪崩（价格瞬间暴跌 15%+）
   - 拔网线式砸盘

3. **趋势判断错误**
   - 在顶部做多，进入长期下跌通道
   - 震荡市连续假突破，每次都亏损 5-8%

### 示例

**无极端保护**：
```
开仓：BTC 10x 多头 @ 70,000
价格：70,000 → 65,000 → 60,000 → 55,000
       -7.1%     -14.3%    -21.4%
       
如果一直没有反向信号：
- 保证金亏损：-71% → -143% → -214%
- 可能触发强平（保证金不足）
- 或者一直等到反向信号出现时已损失惨重
```

**有极端保护（10%）**：
```
开仓：BTC 10x 多头 @ 70,000
价格：70,000 → 63,000（-10%）
       
触发极端保护止损：
- 保证金亏损：-100%（全部亏光）
- 但至少避免了强平和追加保证金
```

## 📝 功能设计

### 优先级

```
极端保护止损（最高优先级）
    ↓
反向信号止损（主要止损方式）
    ↓
价格止盈（继续工作）
```

### 工作逻辑

```python
# 0. 极端保护止损（最高优先级）
if 亏损 >= 10%:
    → 立即止损，理由："触及极端保护止损 -10%（当前 -10.5%）"

# 1. 反向信号止损（主要方式）
if 出现反向信号 and 通过闸门:
    → 信号止损，理由："15m 出现反向信号"

# 2. 价格止盈（保留）
if 盈利 >= 1%/2%/3.5%:
    → 分批止盈
```

### 配置参数

**标准档规则**（`state.py` 第206-221行）：
```python
self.exit_rules = EnhancedExitRules(
    # ... 其他配置 ...
    
    # 极端保护止损（新增）
    max_loss_enabled=True,   # 启用极端保护
    max_loss_pct=10.0,       # 最大亏损10%（价格变动）
)
```

**快进快出档规则**（第222-236行）：
```python
self.exit_rules_quick = EnhancedExitRules(
    # ... 其他配置 ...
    
    # 极端保护止损（新增）
    max_loss_enabled=True,   # 启用极端保护
    max_loss_pct=8.0,        # 快进快出档用更紧的8%
)
```

## 🔧 实现细节

### 1. 规则定义（position_enhanced.py）

```python
@dataclass
class EnhancedExitRules(ExitRules):
    # ... 其他参数 ...
    
    # 极端保护止损
    max_loss_enabled: bool = False   # 是否启用
    max_loss_pct: float = 10.0       # 最大亏损百分比
```

### 2. 检查逻辑（position_enhanced.py 第104-125行）

```python
def check_enhanced(pos: Position, price: float, rules: EnhancedExitRules,
                   max_unrealized: float = None) -> dict | None:
    current_pnl = pos.pnl_pct(price)

    # 0. 极端保护止损（最高优先级）
    if rules.max_loss_enabled and current_pnl <= -rules.max_loss_pct:
        return {
            "action": "max_stop",
            "ratio": 100.0,
            "reason": f"触及极端保护止损 -{rules.max_loss_pct}%（当前 {current_pnl:.2f}%）",
        }

    # 1. 反向信号止损（继续工作）
    # 2. 价格止盈（继续工作）
    # ...
```

### 3. 执行处理（executor.py 第311-318行）

```python
# 处理三级止盈和极端保护止损
if act["action"] in ("tp1", "tp2", "tp3"):
    await self._take_profit(pos, price, act)
elif act["action"] == "max_stop":
    # 极端保护止损
    await self._close(pos, act["reason"], price)
else:
    await self._close(pos, act["reason"], price)
```

## 📊 参数建议

### 按杠杆调整

| 杠杆 | 标准档 max_loss_pct | 快进快出档 max_loss_pct | 保证金亏损 |
|------|-------------------|----------------------|----------|
| 1-3x | 15-20% | 12-15% | -45% ~ -60% |
| 5x   | 12-15% | 10-12% | -60% ~ -75% |
| **10x** | **10%** | **8%** | **-100% ~ -80%** |
| 20x  | 6-8% | 5-6% | -120% ~ -160% |

**当前配置**（10x 杠杆）：
- 标准档：10%（价格亏损 10% = 保证金全亏）
- 快进快出：8%（价格亏损 8% = 保证金亏 80%）

### 按风险偏好调整

**激进配置**（追求利润最大化）：
```python
max_loss_enabled = True
max_loss_pct = 15.0  # 允许更大亏损，给趋势更多空间
```

**特点**：
- ✅ 深回调不会被止损
- ✅ 趋势延续时能赚更多
- ❌ 极端情况损失更大

**保守配置**（控制风险）：
```python
max_loss_enabled = True
max_loss_pct = 6.0   # 更紧的保护，减少单次损失
```

**特点**：
- ✅ 单次损失可控
- ✅ 快速止损离场
- ❌ 正常回调可能被止损

**推荐配置**（平衡）：
```python
# 标准档
max_loss_enabled = True
max_loss_pct = 10.0

# 快进快出档
max_loss_enabled = True
max_loss_pct = 8.0
```

### 按市场环境调整

**震荡市**（建议收紧）：
```python
max_loss_pct = 8.0   # 震荡市假突破多，快速止损
```

**趋势市**（可以放宽）：
```python
max_loss_pct = 12.0  # 趋势市允许深回调
```

**高波动市**（根据 ATR 调整）：
```python
# 如果 24h 波动 > 5%，放宽止损
if ticker.change24h_abs > 5.0:
    max_loss_pct = 12.0
else:
    max_loss_pct = 10.0
```

## ⚖️ 对比分析

### 修改前（纯信号止损）

| 场景 | 价格变动 | 行为 | 结果 |
|------|---------|------|------|
| 深回调 | -8% | 等待反向信号 | ✅ 可能回升盈利 |
| 趋势反转 | -12% | 等待反向信号 | ❌ 亏损 -120%（10x） |
| 信号中断 | -15% | 等待反向信号 | ❌ 亏损 -150%（10x） |
| 闪崩 | -20% | 等待反向信号 | ❌ 强平或巨亏 |

**优势**：给趋势更多空间  
**风险**：极端情况无保护

### 修改后（信号止损 + 极端保护）

| 场景 | 价格变动 | 行为 | 结果 |
|------|---------|------|------|
| 深回调 | -8% | 等待反向信号 | ✅ 可能回升盈利 |
| 趋势反转 | -10% | 触发极端保护 | ⚠️ 止损 -100%（10x） |
| 信号中断 | -10% | 触发极端保护 | ✅ 止损 -100%（避免更大损失） |
| 闪崩 | -15% | 触发极端保护 | ✅ 止损 -100%（避免强平） |

**优势**：极端情况有兜底保护  
**风险**：深回调可能被保护止损

## 🎛️ 使用建议

### 1. 启用极端保护（推荐）

**适用场景**：
- 使用高杠杆（5x 及以上）
- 市场波动剧烈（24h 波动 > 5%）
- 信号系统不够稳定
- 无法 24 小时监控

**配置**：
```python
max_loss_enabled = True
max_loss_pct = 10.0  # 根据杠杆调整
```

### 2. 关闭极端保护（高级用户）

**适用场景**：
- 使用低杠杆（1-3x）
- 趋势市且波动小
- 信号系统高度可靠
- 能够 24 小时监控

**配置**：
```python
max_loss_enabled = False
# 纯信号止损，无极端保护
```

### 3. 监控与调整

**定期检查**（每周）：
```python
# 统计历史数据
被极端保护止损的次数：5 次
其中回升未亏的次数：2 次（误杀）
真正避免更大损失的次数：3 次

误杀率 = 2/5 = 40%
```

**调整策略**：
- 误杀率 > 50% → 放宽 `max_loss_pct`（如从 10% 到 12%）
- 出现强平风险 → 收紧 `max_loss_pct`（如从 10% 到 8%）
- 趋势市 → 放宽（给趋势更多空间）
- 震荡市 → 收紧（快速止损离场）

## 🔍 验证方法

### 1. 查看日志

开仓后，价格大幅下跌：
```
[价格更新] BTC-USDT-SWAP @ 63000（-10.0%）
[极端保护止损] 触及极端保护止损 -10%（当前 -10.0%）
[平仓] BTC-USDT-SWAP 触及极端保护止损 -10%（当前 -10.0%），平仓 0.0005 @ 63000
```

### 2. 前端显示

持仓卡应该显示：
```
持仓：BTC 10x 多头 @ 70,000
当前价：65,000（-7.14%）
止损方式：
  - 主要：反向信号止损
  - 保护：极端保护止损 -10%
极端保护线：63,000（还剩 2.86%）
```

### 3. 测试场景

**场景A：正常回调（不触发）**
```
开仓：70,000
价格：65,500（-6.4%）
→ 未触及 -10%，继续持有
→ 价格回升到 72,000
→ 第1档止盈 @ 70,700
```

**场景B：深回调（触发保护）**
```
开仓：70,000
价格：63,000（-10.0%）
→ 触发极端保护止损
→ 立即平仓 @ 63,000
→ 保证金亏损 -100%
```

**场景C：反向信号先到（不触发保护）**
```
开仓：70,000
价格：65,000（-7.1%）
→ 15m 出现反向信号，通过闸门
→ 信号止损 @ 65,000
→ 保证金亏损 -71%
→ 极端保护未触发（信号止损优先）
```

## 📈 完整工作流程

```
开仓：BTC 10x 多头 @ 70,000
配置：
  - 主要止损：反向信号（通过闸门）
  - 极端保护：-10%（63,000）
  - 止盈：1%/2%/3.5%

价格变动：
  
  场景1：正常盈利
  70,000 → 70,700（+1%）→ 第1档止盈 30%
         → 71,400（+2%）→ 第2档止盈 40%
         → 72,450（+3.5%）→ 第3档止盈 剩余
  
  场景2：小幅回调后反弹
  70,000 → 68,000（-2.9%）→ 继续持有（等待信号）
         → 72,000（+2.9%）→ 第1档止盈
  
  场景3：深回调后反弹（未触发保护）
  70,000 → 64,000（-8.6%）→ 继续持有（未到 -10%）
         → 71,000（+1.4%）→ 第1档止盈
  
  场景4：深回调触发保护
  70,000 → 63,000（-10.0%）→ 极端保护止损
  理由："触及极端保护止损 -10%（当前 -10.0%）"
  保证金亏损：-100%
  
  场景5：反向信号先到
  70,000 → 66,000（-5.7%）→ 15m 反向信号通过闸门
                          → 信号止损 @ 66,000
  理由："15m 出现反向信号"
  保证金亏损：-57%
```

## 📝 总结

### 现在的三层保护

1. **价格止盈**（继续工作）
   - 1% → 平 30%
   - 2% → 再平 40%
   - 3.5% → 剩余全平

2. **反向信号止损**（主要止损方式）
   - 反向信号 + 通过闸门 → 平仓
   - 假信号被屏蔽 → 不平仓

3. **极端保护止损**（新增，兜底保护）
   - 亏损 ≥ 10%（标准档）或 8%（快进快出）→ 强制平仓
   - 最高优先级，避免强平和巨额亏损

### 优势

✅ **平衡灵活性和安全性**
- 正常情况下由信号驱动（给趋势更多空间）
- 极端情况下强制止损（避免灾难性损失）

✅ **适应高杠杆交易**
- 10x 杠杆时，10% 价格亏损 = 100% 保证金亏损
- 极端保护避免强平

✅ **可配置**
- 可以根据杠杆、市场环境、风险偏好调整
- 可以完全关闭（对于低杠杆或高级用户）

### 风险

⚠️ **可能误杀深回调**
- 如果价格回调 10% 后反弹，会被保护止损
- 缓解：根据历史数据调整 `max_loss_pct`

⚠️ **不能完全避免强平**
- 如果价格暴跌速度过快（如闪崩），可能来不及止损
- 缓解：使用合理杠杆（≤10x），避免满仓

**重启服务，极端保护生效！** 🚀
