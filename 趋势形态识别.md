以下是一套可直接用于写代码（Python/Pandas/C# 等）的量化识别逻辑与公式体系：一、 核心输入变量定义设当前 K 线序列为 $i = 1, 2, \dots, N$，包含以下数据：$H_i$: 第 $i$ 根 K 线的最高价 (High)$L_i$: 第 $i$ 根 K 线的最低价 (Low)$C_i$: 第 $i$ 根 K 线的收敛/收盘价 (Close)二、 核心算法公式1. 高低点极值提取 (Pivot / ZigZag)判定趋势前，需提取出局部波段的高点 $P_{high}$ 与低点 $P_{low}$。采用周期为 $k$（如 $k=5$）的局部极值算法：局部高点 (HH/LH): 当 $H_i = \max(H_{i-k}, \dots, H_{i+k})$ 时，$H_i$ 为关键波峰。局部低点 (HL/LL): 当 $L_i = \min(L_{i-k}, \dots, L_{i+k})$ 时，$L_i$ 为关键波谷。提取出最近 $m$ 个波峰序列 $\{PH_1, PH_2, \dots, PH_m\}$ 和波谷序列 $\{PL_1, PL_2, \dots, PL_m\}$（下标越大代表时间越近）。2. 高低点变化率公式 (Slope & Direction)对波峰和波谷分别计算线性回归斜率（或简单差值）：$$\Delta PH = PH_m - PH_{m-1}$$$$\Delta PL = PL_m - PL_{m-1}$$$\Delta PH > 0$ 且 $\Delta PL > 0 \rightarrow$ 整体向上$\Delta PH < 0$ 且 $\Delta PL < 0 \rightarrow$ 整体向下3. 结构重叠度算法 (Overlap Ratio - 区分单边与震荡)定义相邻波段的重叠区域长度。设前一个波峰为 $PH_{m-1}$，当前波谷为 $PL_m$：上行通道重叠深度：$$OR_{up} = \frac{\max(0, PH_{m-1} - PL_m)}{PH_m - PL_{m-1}}$$下行通道重叠深度：$$OR_{down} = \frac{\max(0, PH_m - PL_{m-1})}{PH_{m-1} - PL_m}$$当 $OR$ 接近 0 时说明几乎无回调重叠（强趋势）；当 $OR > 0.3$ 时说明回调较深（震荡趋势）。4. 趋势强度辅助：ADX (Average Directional Index)计算真实波幅 $TR$ 与方向动量 $DIn$：$$TR_i = \max(H_i - L_i, \vert{}H_i - C_{i-1}\vert{}, \vert{}L_i - C_{i-1}\vert{})$$$$+DM_i = \begin{cases} H_i - H_{i-1} & \text{if } H_i - H_{i-1} > L_{i-1} - L_i \text{ and } H_i - H_{i-1} > 0 \\ 0 & \text{otherwise} \end{cases}$$$$-DM_i = \begin{cases} L_{i-1} - L_i & \text{if } L_{i-1} - L_i > H_i - H_{i-1} \text{ and } L_{i-1} - L_i > 0 \\ 0 & \text{otherwise} \end{cases}$$通过 $N$ 周期平滑后得到 $+DI_N, -DI_N$ 与 $ADX$。三、 形态判定决策树 (Quant Logic)根据计算出的 $\Delta PH, \Delta PL, OR, ADX$ 进行状态赋值：Plaintext    ┌───────────────────────────┐
    │  ADX < 20 或 均线斜率 ≈ 0   │ ──(是)──> 【无明显趋势】
    └─────────────┬─────────────┘
                  │ (否)
    ┌─────────────┴─────────────┐
    │  ΔPH > 0  且  ΔPL > 0     │ ──(是)───┐
    └─────────────┬─────────────┘          │
                  │ (否)                   ▼
                  │         ┌──────────────────────────────┐
                  │         │  OR_up < 0.25 且 ADX > 25    │ ──(是)──> 【上行趋势】
                  │         └──────────────┬───────────────┘
                  │                        │ (否)
                  │                        └─────────────────────────> 【震荡上行】
                  ▼
    ┌───────────────────────────┐
    │  ΔPH < 0  且  ΔPL < 0     │ ──(是)───┐
    └─────────────┬─────────────┘          │
                  │ (否)                   ▼
                  │         ┌──────────────────────────────┐
                  │         │  OR_down < 0.25 且 ADX > 25  │ ──(是)──> 【下行趋势】
                  │         └──────────────┬───────────────┘
                  │                        │ (否)
                  │                        └─────────────────────────> 【震荡下行】
                  ▼
          【无明显趋势 (宽幅震荡)】
四、 识别条件汇总表形态极值条件 (ΔPH,ΔPL)均线条件 (如 MA20 & MA60)重叠度 / ADX 条件上行趋势$\Delta PH > 0$ 且 $\Delta PL > 0$$MA_{20} > MA_{60}$ 且 斜率 $> k$$OR_{up} < 0.25$ 且 $ADX \ge 25$震荡上行$\Delta PH > 0$ 且 $\Delta PL > 0$$MA_{20} > MA_{60}$ 且 斜率 $> 0$$OR_{up} \ge 0.25$ 或 $ADX < 25$下行趋势$\Delta PH < 0$ 且 $\Delta PL < 0$$MA_{20} < MA_{60}$ 且 斜率 $< -k$$OR_{down} < 0.25$ 且 $ADX \ge 25$震荡下行$\Delta PH < 0$ 且 $\Delta PL < 0$$MA_{20} < MA_{60}$ 且 斜率 $< 0$$OR_{down} \ge 0.25$ 或 $ADX < 25$无明显趋势交叉不固定 ($\Delta PH \cdot \Delta PL \le 0$)$MA_{20} \approx MA_{60}$（走平缠绕）$ADX < 20$

补上这个弱点：虽然 4H + 1H 同向过滤很有效，但在以下场景需要特别注意：4H 处于“无明显趋势（横盘震荡）”时：当 4H 价格在狭幅箱体内摆动，4H Supertrend 会频繁红绿交替（锯齿磨损）。此时 1H 跟着 4H 做同向单，容易发生“买在 4H 箱体顶部，卖在 4H 箱体底部”的连续亏损。解法：增加一个辅助过滤器（如 4H 级别的 $ADX < 20$ 或布林带走平），当 4H 属于无趋势震荡时，暂停 Supertrend 突破信号，改用网格或高抛低吸。
4H 趋势尾声的反转回调：当 4H 长期上行后出现见顶信号，1H 第一次转红时，4H 可能依然显示为绿色（滞后性）。这会导致你错过最好的空单位置，甚至在 1H 最后一棒多单中吃满止损。解法：以 1H Supertrend 跌破作为离场/止损线，绝对不要用 4H 的 Supertrend 来做 1H 单子的止损（否则止损空间太大，盈亏比极差）。

> **形态识别页的基础信号来源（实现约定）**
> 本页主图的基础信号采用 **原始 SuperTrend 信号**（见 `supertrend原始代码.md`）：`ATR Period = 10`、`ATR Factor = 3.0`、`src = hl2`、`changeATR = true`（即 Pine 的 `ta.atr` 经典实现，`trend` 在 +1/-1 间翻转即出信号）。
> 该参数**仅用于形态识别页**，与首页默认 SuperTrend（`Periods = 15, Multiplier = 9.1`）相互独立、互不影响。信号逻辑：主图原始 SuperTrend 翻转出信号时，再用 4h K 线做趋势形态识别；4h 形态与信号**同向**才允许下单，方向**相反**则不下单。
> **2026-09 变更**：**已去掉「4h 无明显趋势」的拦截** —— 4h 形态判定为「无明显趋势（dir=0）」时**正常下单**。
> 依据 BTC/ETH × 2025全年/2026H1 四窗口回测：放开 dir=0 可提升收益，且 ETH 2026H1 最大回撤
> 由 9.76% 降至 4.40%、最长连亏 3→2。现在仅 `4h 数据不足`（形态尚未生成）与 `4h 反向` 会拦单。

<!-- 过滤规则  -->
Rule 1：连续翻转过滤
if bars_since_last_flip < 20:
    reject

原因：

震荡。

Rule 2：波动异常过滤
if ATR_percent > 0.8:
    reject

原因：

追涨杀跌。

Rule 3：箱体错误位置过滤

多：

if range_position <0.3:
    reject

空：

if range_position >0.7:
    reject

原因：

不是突破，是反抽。

Rule 4：极端K过滤
if candle_range_ATR >3:
    reject

原因：

容易是一根情绪K。